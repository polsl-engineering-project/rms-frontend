# Local development (optional override)
# If you want to bypass the proxy and hit the backend directly:
# VITE_API_URL=http://localhost:8080

# Or if backend is on a different port:
# VITE_API_URL=http://localhost:3000

# Leave empty to use Vite proxy (recommended):
VITE_API_URL=
